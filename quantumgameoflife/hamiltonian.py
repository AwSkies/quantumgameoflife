from .cells import *
def hamiltonian_boolean(cell_matrix):
    '''
    determine whether hamiltonian should be activated
    return boolean matrix of the same size
    '''
    boolean_matrix = np.zeros(cell_matrix.shape, dtype = int)

    return boolean_matrix

